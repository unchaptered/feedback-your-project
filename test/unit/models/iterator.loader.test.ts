import * as IteratorLoader from '../../../src/models/iterator.loader';


describe ('Iterator Loader', () => {

    it ('has 1 properties', () => expect(Object.keys(IteratorLoader).length).toBe(1));

});